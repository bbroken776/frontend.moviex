const config = {
  links: [
    { title: 'Home', route: '/' },
    { title: 'Movies', route: '/movies' },
  ],
  userLinks: [
    { title: 'Profile', route: '/me/profile' },
  ]
};

export default config;
